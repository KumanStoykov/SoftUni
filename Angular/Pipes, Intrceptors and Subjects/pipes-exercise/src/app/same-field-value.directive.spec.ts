import { SameFieldValueDirective } from './same-field-value.directive';

describe('SameFieldValueDirective', () => {
  it('should create an instance', () => {
    const directive = new SameFieldValueDirective();
    expect(directive).toBeTruthy();
  });
});
